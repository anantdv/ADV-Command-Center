import { AlertCircle, RefreshCw } from 'lucide-react'
export function ErrorState({ retry, message = 'We could not load this workspace data.' }: { retry: () => void; message?: string }) { return <div className="card flex min-h-72 flex-col items-center justify-center p-8 text-center"><span className="flex size-12 items-center justify-center rounded-2xl bg-rose-50 text-rose-600"><AlertCircle/></span><h2 className="mt-4 font-bold text-slate-900">Unable to load data</h2><p className="mt-2 max-w-sm text-sm text-slate-500">{message}</p><button onClick={retry} className="btn-secondary mt-5"><RefreshCw size={15}/>Try again</button></div> }
