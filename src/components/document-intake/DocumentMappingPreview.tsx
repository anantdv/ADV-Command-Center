import type { DocumentMappingPreview as Preview } from '../../types/documentIntake'
import { ExtractedLineItemsTable } from './ExtractedLineItemsTable'

export function DocumentMappingPreview({preview,onConfirm,onCancel,busy=false}:{preview:Preview;onConfirm?:()=>void;onCancel?:()=>void;busy?:boolean}){
 const fields=Object.entries(preview.draft_payload).filter(([key])=>key!=='items')
 return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs uppercase tracking-wide text-slate-400">{preview.source_document_type.replaceAll('_',' ')}</p><h3 className="text-lg font-bold text-slate-900">Create Draft {preview.target_doctype}</h3></div><span className="rounded-full bg-amber-100 px-3 py-1 text-xs font-semibold text-amber-700">Confirmation required</span></div>{preview.warnings.length>0&&<div className="mt-4 rounded-xl bg-amber-50 p-3 text-xs text-amber-800">{preview.warnings.join(' ')}</div>}<dl className="mt-4 grid gap-3 sm:grid-cols-2">{fields.map(([key,value])=><div key={key} className="rounded-xl bg-slate-50 p-3"><dt className="text-[10px] uppercase text-slate-400">{key.replaceAll('_',' ')}</dt><dd className="mt-1 text-sm font-medium text-slate-800">{String(value)}</dd></div>)}</dl><div className="mt-4"><ExtractedLineItemsTable items={preview.extracted_fields.items}/></div>{preview.missing_fields.length>0&&<div className="mt-4 rounded-xl bg-rose-50 p-3 text-xs text-rose-700">Missing required fields: {preview.missing_fields.map(field=>String(field.label||field.fieldname)).join(', ')}</div>}<div className="mt-5 flex justify-end gap-2">{onCancel&&<button onClick={onCancel} className="btn-secondary h-10 px-4 text-sm">Cancel</button>}{onConfirm&&<button onClick={onConfirm} disabled={busy||!preview.confirmation_id||preview.missing_fields.length>0} className="btn-primary h-10 px-4 text-sm">Confirm Create Draft</button>}</div></section>
}
