import { ExternalLink } from 'lucide-react'
import type { RAGCitation } from '../../types/knowledge'
import { KnowledgeSourceBadge } from './KnowledgeSourceBadge'
export function CitationList({citations}:{citations:RAGCitation[]}){if(!citations.length)return <div className="rounded-xl bg-amber-50 p-3 text-xs font-semibold text-amber-700">No approved source found. Escalation recommended.</div>;return <div className="space-y-2"><p className="text-[10px] font-bold uppercase tracking-wider text-slate-400">Approved sources</p>{citations.map(c=><div key={c.citationId} className="rounded-xl border border-slate-200 bg-white p-3"><div className="flex flex-wrap items-center gap-2"><b className="text-xs text-slate-800">[{c.citationId}] {c.title}</b>{c.sourceType&&<KnowledgeSourceBadge type={c.sourceType}/>}<ExternalLink size={11} className="ml-auto text-slate-300"/></div><p className="mt-2 text-[11px] leading-5 text-slate-500">{c.snippet}</p><span className="mt-2 block text-[10px] font-semibold text-indigo-500">{c.citationLabel}</span></div>)}</div>}
