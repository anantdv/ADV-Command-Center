import { useState } from 'react'
import { X } from 'lucide-react'
import { useCreateKnowledgeSource } from '../../hooks/api/useKnowledge'
export function KnowledgeUploadDialog({open,onClose}:{open:boolean;onClose:()=>void}){const create=useCreateKnowledgeSource();const [title,setTitle]=useState('');const [content,setContent]=useState('');if(!open)return null;return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4"><form className="w-full max-w-xl rounded-2xl bg-white p-6 shadow-2xl" onSubmit={e=>{e.preventDefault();create.mutate({title,content,sourceType:'sop_document'},{onSuccess:onClose})}}><div className="flex items-center"><h2 className="flex-1 font-bold">Register private knowledge</h2><button type="button" onClick={onClose}><X/></button></div><p className="mt-1 text-xs text-slate-400">New sources remain draft until a manager approves and ingests them.</p><input className="mt-5 w-full rounded-xl border p-3 text-sm" placeholder="Source title" value={title} onChange={e=>setTitle(e.target.value)} required/><textarea className="mt-3 min-h-40 w-full rounded-xl border p-3 text-sm" placeholder="Approved SOP or training content" value={content} onChange={e=>setContent(e.target.value)} required/><button className="btn-primary mt-4" disabled={create.isPending}>Create draft</button></form></div>}
