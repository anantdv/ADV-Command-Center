import { ArrowRight, Sparkles } from 'lucide-react'
import { useNavigate } from 'react-router-dom'
import type { Module } from '../../types'
import { PermissionBadge } from '../common/Badges'

const tones: Record<string,string> = { indigo:'bg-indigo-50 text-indigo-600',blue:'bg-blue-50 text-blue-600',amber:'bg-amber-50 text-amber-600',emerald:'bg-emerald-50 text-emerald-600',violet:'bg-violet-50 text-violet-600',cyan:'bg-cyan-50 text-cyan-600',sky:'bg-sky-50 text-sky-600',rose:'bg-rose-50 text-rose-600',slate:'bg-slate-100 text-slate-600',orange:'bg-orange-50 text-orange-600' }
export function ModuleCard({ module }: { module: Module }) { const navigate=useNavigate(); const Icon=module.icon;const readable=module.permissions?.canRead!==false;return <article className="card group p-5 transition duration-200 hover:-translate-y-1 hover:shadow-lg"><div className="flex items-start justify-between"><span className={`flex size-11 items-center justify-center rounded-2xl ${tones[module.color]}`}><Icon size={21}/></span>{module.permissions&&!module.permissions.canWrite?<PermissionBadge allowed={false} reason={module.permissions.reason}/>:<span className="rounded-full bg-slate-50 px-2.5 py-1 text-[10px] font-bold text-slate-400">ERP MODULE</span>}</div><h3 className="mt-5 font-[Manrope] text-base font-bold">{module.name}</h3><p className="mt-2 min-h-10 text-xs leading-5 text-slate-500">{module.description}</p><div className="my-5 rounded-xl bg-slate-50 p-3"><p className="font-[Manrope] text-lg font-bold text-slate-900">{module.metric}</p><p className="mt-0.5 text-[10px] font-semibold text-slate-400">{module.metricLabel}</p></div><div className="flex gap-2"><button disabled={!readable} title={!readable?module.permissions?.reason:undefined} onClick={()=>navigate(`/modules/${module.slug}`)} className="btn-secondary flex-1 disabled:cursor-not-allowed disabled:opacity-50">Open Module<ArrowRight size={14}/></button><button disabled={!readable} onClick={()=>navigate('/command-center')} className="flex size-9 items-center justify-center rounded-xl bg-indigo-50 text-indigo-600 transition hover:bg-indigo-100 disabled:cursor-not-allowed disabled:opacity-50" title={readable?'Ask AI':module.permissions?.reason}><Sparkles size={16}/></button></div></article> }
