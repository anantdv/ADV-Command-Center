import { useState } from 'react'
import { useEscalateSupport } from '../../hooks/api/useSupport'
export function TicketEscalationDialog({question,onClose}:{question:string;onClose:()=>void}){const [subject,setSubject]=useState(`ERPNext help: ${question.slice(0,60)}`);const escalate=useEscalateSupport();return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4"><form className="w-full max-w-lg rounded-2xl bg-white p-6" onSubmit={e=>{e.preventDefault();escalate.mutate({question,subject,description:question,priority:'Medium'},{onSuccess:onClose})}}><h2 className="font-bold">Escalate to human support</h2><input className="mt-4 w-full rounded-xl border p-3 text-sm" value={subject} onChange={e=>setSubject(e.target.value)}/><textarea className="mt-3 w-full rounded-xl border p-3 text-sm" value={question} readOnly/><div className="mt-4 flex gap-2"><button className="btn-primary">Create ticket</button><button type="button" className="btn-secondary" onClick={onClose}>Cancel</button></div></form></div>}
