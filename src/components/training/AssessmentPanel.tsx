import type { GeneratedAssessment } from '../../types/training'
export function AssessmentPanel({assessment}:{assessment?:GeneratedAssessment}){return <section className="card p-5"><h3 className="font-bold">Assessment</h3>{assessment?.questions.map((q,i)=><div key={q.questionId} className="mt-4 border-t pt-4"><p className="text-sm font-semibold">{i+1}. {q.question}</p>{q.options.map(option=><label key={option} className="mt-2 flex gap-2 text-xs text-slate-600"><input type="radio" name={q.questionId}/>{option}</label>)}</div>)||<p className="mt-2 text-xs text-slate-400">Generate a quiz from an approved, ingested training source.</p>}</section>}
