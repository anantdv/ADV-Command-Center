export function MailFilters({filters,onChange}:{filters:Record<string,unknown>;onChange:(v:Record<string,unknown>)=>void}){return <div className="flex flex-wrap gap-2 pt-3">{[['unread','Unread'],['linked','Linked'],['has_attachments','Attachments']].map(([key,label])=><button key={key} onClick={()=>onChange({...filters,[key]:!filters[key]})} className={`rounded-full border px-2.5 py-1 text-[10px] font-bold ${filters[key]?'border-indigo-200 bg-indigo-50 text-indigo-700':'border-slate-200 text-slate-500'}`}>{label}</button>)}</div>}
