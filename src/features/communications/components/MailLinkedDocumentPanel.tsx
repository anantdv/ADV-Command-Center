import {useState} from 'react'
import {ExternalLink,Link2} from 'lucide-react'
import {communicationApi} from '../services/communicationApi'
export function MailLinkedDocumentPanel({communicationName,doctype,name,onLinked}:{communicationName:string;doctype?:string|null;name?:string|null;onLinked:()=>void}){const[editing,setEditing]=useState(false);const[dt,setDt]=useState(doctype||'');const[dn,setDn]=useState(name||'');const[busy,setBusy]=useState(false);const save=async()=>{setBusy(true);try{await communicationApi.link(communicationName,dt,dn);setEditing(false);onLinked()}finally{setBusy(false)}};return <div className="rounded-xl border border-slate-200 bg-slate-50 p-3"><div className="flex items-center gap-2"><Link2 size={14} className="text-violet-500"/><p className="text-[10px] font-bold uppercase tracking-wider text-slate-500">Linked ERPNext document</p></div>{!editing?<div className="mt-2 flex items-center justify-between"><p className="text-xs font-semibold text-slate-700">{name?`${doctype} · ${name}`:'Not linked'}</p><button onClick={()=>setEditing(true)} className="text-[10px] font-bold text-indigo-600">{name?'Relink':'Link'}</button></div>:<div className="mt-3 grid gap-2 sm:grid-cols-2"><input value={dt} onChange={e=>setDt(e.target.value)} placeholder="DocType" className="h-9 rounded-lg border px-2 text-xs"/><input value={dn} onChange={e=>setDn(e.target.value)} placeholder="Document name" className="h-9 rounded-lg border px-2 text-xs"/><button disabled={!dt||!dn||busy} onClick={save} className="btn-primary h-9 text-xs">Save link</button><button onClick={()=>setEditing(false)} className="btn-secondary h-9 text-xs">Cancel</button></div>}</div>}
