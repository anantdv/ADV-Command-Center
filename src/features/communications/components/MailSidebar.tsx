import {FileText,Inbox,Link2,Send,Settings,Star,Tags} from 'lucide-react'
import type {MailFolder} from '../types/communication.types'
const items:[MailFolder,string,typeof Inbox][]=[['inbox','Inbox',Inbox],['sent','Sent',Send],['drafts','Drafts',FileText],['starred','Starred',Star],['linked','Linked',Link2],['templates','Templates',Tags]]
export function MailSidebar({active,onChange,onCompose}:{active:MailFolder;onChange:(v:MailFolder)=>void;onCompose:()=>void}){return <aside className="border-b border-slate-200 bg-white p-3 md:w-48 md:border-b-0 md:border-r"><button onClick={onCompose} className="btn-primary w-full">Compose</button><nav className="mt-4 flex gap-1 overflow-x-auto md:block md:space-y-1">{items.map(([id,label,Icon])=><button key={id} onClick={()=>onChange(id)} className={`flex shrink-0 items-center gap-2 rounded-lg px-3 py-2 text-xs font-semibold md:w-full ${active===id?'bg-indigo-50 text-indigo-700':'text-slate-500 hover:bg-slate-50'}`}><Icon size={15}/>{label}</button>)}</nav><button className="mt-4 hidden items-center gap-2 px-3 py-2 text-xs text-slate-400 md:flex"><Settings size={15}/>Settings</button></aside>}
